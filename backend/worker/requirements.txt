# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.1

# --- JWT / 보안 ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4      # 비밀번호 해싱
bcrypt<4.0
python-multipart==0.0.9     # Form/OAuth callback 처리에 필요

# --- DB / ORM ---
SQLAlchemy>=2.0
alembic>=1.13
redis
rq
pymysql

# --- OAuth2 클라이언트 ---
authlib==1.3.2

# --- 기타 ---
pydantic[email]
pydantic-settings>=2.3.0
watchfiles

# --- AWS SDK for Python ---
boto3>=1.34,<2.0
botocore>=1.34,<2.0  # (boto3 설치 시 함께 설치됨)

# --- fro email ---
jinja2>=3.1         # (옵션) HTML 템플릿 렌더링용


#--------- worker ----------------
# Retry Utility
tenacity==8.2.3
